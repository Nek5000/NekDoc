sudo: required

install:
  - sudo pip install sphinx
  - sudo pip install sphinx_rtd_theme

script:
  - make clean
  - make html | tee logfile
  - sed 's/[^0-9]*//g' logfile | tee log2
  - sed '/^\s*$/d' log2 | tee log3
  - NUMBER=$(tail -1 log3)
  - if (( $NUMBER > 4 )); then exit 1; fi

